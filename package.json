import { defineConfig } from 'astro/config';
import react from '@astrojs/react';
import tailwind from '@astrojs/tailwind'; // <-- Должно быть так
import { sanity } from '@sanity/astro';

export default defineConfig({
  integrations: [
    react(),
    tailwind(), // <-- И здесь
    sanity({
      projectId: 'd4fi998k',
      dataset: 'production',
      studioPath: '/admin',
      useCdn: false,
    }),
  ],
});
